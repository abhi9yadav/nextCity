import React from 'react';

const WorkerDashboard = ({ title, value }) => {
  return (
    <div className="card">
      <h1>Welcome to the Worker Dashboard</h1>
    </div>
  );
};

export default WorkerDashboard;
